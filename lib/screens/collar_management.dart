import 'package:flutter/material.dart';

class Collar_Management extends StatelessWidget{
  const Collar_Management({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold (
        body: const Center(
          child: Text('Collar Management Page'),
        )
    );
  }
}